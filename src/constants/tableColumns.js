import React from 'react'; // eslint-disable-line
import { parseAge, momentFormatDate } from 'app/services/format';
import { SEMESTER_TYPES, YEARS } from './options';

export const catalogsColumns = [
  {
    translate: 'catalogs',
    dataIndex: 'name',
  },
];
